<% if(deployedDomains.length <= 0) { %> 
  <span style="font-weight: 600">Lander Modified</span>. Save is Required.
<% } else { %>
  <span style="font-weight: 600">Lander Modified</span>. Deploy is Required.
<% } %>